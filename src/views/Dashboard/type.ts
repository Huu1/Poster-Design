export type TwhSize = {
  width: number;
  height: number;
};
export type TScale = {
  x: number;
  y: number;
};
export type Tclip = {
  clipX: number;
  clipY: number;
};
export type TOffset = {
  offsetX: number;
  offsetY: number;
};

export enum EventType {
  photo = "photo",
}
